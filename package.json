{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "The kryptoknow dashboard",
  "scripts": {
    "clean": "rm -Rf ./dist",
    "build": "mkdir -p dist && cp *.html dist && cp -r assets dist && cp -r images dist && cp CNAME dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "dev": "nodemon ./index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KryptoKnow/dashboard.git"
  },
  "keywords": [],
  "author": "kryptoknow",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.16.3",
    "gh-pages": "^1.2.0"
  },
  "bugs": {
    "url": "https://github.com/KryptoKnow/dashboard/issues"
  },
  "homepage": "https://github.com/KryptoKnow/dashboard#readme"
}
